# Tower Connect Studio

Tower Connect Studio is a React + Vite project for a polished, animated studio-style website.

## Getting Started

Install dependencies and run the app with:

```bash
npm install
npm run dev
```

## Scripts

- `npm run dev` starts the local development server.
- `npm run build` creates a production build.
- `npm run lint` runs the linter.
- `npm run test` runs the test suite.





Innovative Structural
Solutions for Wireless
Infrastructure---------Hero heading 1